var modal = $("#traineeProgressModal");
modal.find(".modal-body").html("<%= j render 'trainee_progress' %>")
modal.find(".progress-bar").each(function(){
  var percent = $(this).data("percent");
  $(this).css("width", percent);
});
